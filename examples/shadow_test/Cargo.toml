[package]
name = "shadow_test"
version.workspace = true
edition.workspace = true

[features]
hot-reload = ["esox_engine/hot-reload"]

[dependencies]
esox_engine = { workspace = true, features = ["ui"] }
tracing-subscriber = { workspace = true }
