[package]
name = "editor"
version.workspace = true
edition.workspace = true

[features]
hot-reload = ["esox_engine/hot-reload"]

[dependencies]
esox_engine = { workspace = true, features = ["ui", "rapier", "serialization"] }
tracing-subscriber = { workspace = true }
rfd = "0.15"
